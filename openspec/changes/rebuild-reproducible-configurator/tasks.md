# Tasks — rebuild-reproducible-configurator

Implementation plan for the staged Go rebuild of `alex-cachyos`, built alongside the preserved Bash tree. Every task is an unchecked, actionable checkbox. Code-bearing units follow RED → GREEN → TRIANGULATE → REFACTOR with recorded evidence. Bash validation runs in every unit. Delivery is verification-first: every unit ends by preparing a bounded work-unit diff; staging, committing, and branch/PR creation are separate steps taken only on explicit user authorization (see Delivery rules).

## Review Workload Record

| Field | Value |
|-------|-------|
| Integrated at closure | 52 commits; 122 files; 22,105 insertions |
| Numeric line cap | None — explicitly removed by the user |
| Review-unit policy | Split only by behavioral cohesion and material reviewer risk; line count is non-binding |
| PR construction | Deferred; no PR or push has been authorized |
| Chain strategy if later resumed | `stacked-to-main` (selected by user) |

```text
Decision needed before apply: No (resolved: stacked-to-main)
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
Numeric line cap: None (removed by explicit user decision)
```

## Delivery rules (bind every unit)

- **Behavioral review scope:** keep units cohesive and reasonably reviewable, but never split, compress tests, or redesign implementation to satisfy a numeric line count. The user explicitly removed the 400-line cap. Ask only when a unit expands into unrelated behavior or materially changes review risk.
- **Chain strategy recorded before apply:** the user selected `stacked-to-main`. Each authorized review slice targets `main` sequentially and depends on the prior slice landing first. This records review boundaries only; branch/PR construction still requires separate explicit authorization.
- **Commit/PR deferral (hard rule, binds every unit):** no task in this plan stages, commits, pushes, or creates branches/PRs. Each unit ends by verifying its work and preparing a **bounded work-unit diff** (unit-owned path list plus `git diff --stat` summary) as a handoff artifact. Staging and committing happen only after a separate explicit user authorization, staging the declared unit-owned paths explicitly and never `git add -A`. Branch and PR construction happen only on a separate explicit user request, consistent with applicable repository policy and the user-recorded chain strategy. Review slices and chain boundaries in this plan are proposals, not scheduled Git actions.
- **RDD review authority:** review execution follows the user's receipt-driven-development switch and the harness's native RDD authority at implementation time. This plan records per-unit evidence and review scope, but its checklists neither fabricate review authority nor schedule automatic per-unit reviews merely because a plan exists; with the switch disabled, delivery follows ordinary repository policy and reports `disabled/unmanaged`, never a fabricated approval.
- **Rollback semantics:** for an uncommitted unit, `Revert` in the unit map means discarding that unit's working-tree diff by its declared unit-owned paths; if the user separately authorized committing a unit, `Revert` means reverting that unit's commit(s). Neither path touches unrelated dirty work.
- **Dirty-worktree preservation (hard rule):** never run `git add -A`, `git stash`, `git clean`, `git reset --hard`, `git checkout .`, or any destructive cleanup against the active `alex-cachyos` worktree or the `gentle-ai`/`gentle-pi` clones. Any staging (only after the separate authorization above) uses **explicit unit-owned paths**; pre-existing dirty changes are never staged, committed, or stashed by these tasks.
- **Transition guard (runs in every unit's verification):** `bash -n apply bin/alex-cachyos-webapp-launch lib/*.sh modules/*.sh && python3 -c 'import json; from pathlib import Path; [json.load(p.open()) for p in Path("profiles").glob("*.json")]'` must exit 0 throughout.
- **Test-first despite `strict_tdd: false`:** each code-bearing unit writes failing tests first (RED), implements minimally (GREEN), adds a discriminating second case (TRIANGULATE), then restructures (REFACTOR). Evidence = test command + observed pass/fail in the apply log.
- **`openspec/config.yaml` gate:** the `go test ./...` runner entry is added only once Go tests exist and pass locally (WU-1); the asset-drift check is added when the checker exists (WU-2).
- **Generated copies:** embedded asset copies under `internal/assets/data/` are mechanical duplications of declared Bash sources; each unit declares only the copies its behavior consumes, keeping per-unit diffs bounded and reviewable.
- **No product mutation of `gentle-ai`/`gentle-pi`:** both repos are inspected dependencies and runtime authorities; nothing in this plan patches them.

## Unit map

| Unit | Behavior slice | Specs | Design | Initial forecast (non-binding) | Rollback boundary |
|------|----------------|-------|--------|-------------------------------|-------------------|
| WU-0 | Baseline safety evidence | staged-migration | §14 | ~0 | Read-only |
| WU-1 | Go scaffold, CLI surface, XDG paths, runner config gate | convergence-commands, receipts, staged-migration | §2.1, §6.1, §13.2 | ~350 | Revert unit diff, or unit commits if the user separately authorized committing; Bash untouched |
| WU-2 | sync-assets + embedded copies + drift check | staged-migration, catalog | §2.2 | ~300 + bounded copies | Revert; `go generate` reproduces copies |
| WU-3 | Catalog schema, decode, pre-mutation validation | catalog | §3.1 | ~400 | Revert; no mutation possible (pure validation) |
| WU-4 | Merge, normalization, digest | catalog | §3.2 | ~380 | Revert |
| WU-5 | Source-specific pins, host resolution, initial catalog | catalog, pi-configuration | §3.3, §3.4 | ~380 + ~150 YAML | Revert; catalog files are new |
| WU-6 | Planner + fake runner + galaxy fixture | planner, convergence-commands | §4.1, §13.1 | ~390 | Revert; fixture-only, no hardware |
| WU-7 | Receipt schema + atomic store + current index | receipts | §6.2, §6.3 | ~390 | Revert; no receipts read by prod paths yet |
| WU-8 | Adoption backups + inverse preconditions | convergence-commands, security-boundaries | §7.1, §6.4 | ~360 | Revert; no host files touched in tests |
| WU-9 | Non-destructive Git port | worktree-safety, pi-source-checkouts, catalog | §7.2, §7.3 | ~390 | Revert; temporary test repos only |
| WU-10 | apply orchestration: lock, idempotency, elevation, dry-run | convergence-commands | §4.2, §4.3, §5 | ~380 | Revert; fake ports only |
| WU-11 | check / adopt / rollback / checkpoint / receipt surfaces | convergence-commands, catalog, worktree-safety | §5, §6.4, §7.3 | ~380 | Revert |
| WU-12 | Platform bootstrap + fingerprint | cachyos-platform | §11 | ~400 | Revert; golden fake-runner plans only |
| WU-13 | Platform devtools + apps + vicinae | cachyos-platform | §11 | ~380 | Revert |
| WU-14 | Platform desktop + verify + hardware gate | cachyos-platform, acceptance-verification | §11, §13.2 | ~380 | Revert |
| WU-15 | Pi exact packages + user-owned renders + 23 routes | pi-configuration, model-routes | §9.1, §9.2 | ~400 | Revert |
| WU-16 | Managed assets + AGENTS/APPEND ownership | pi-configuration, security-boundaries | §9.3, §7.1 | ~350 | Revert |
| WU-17 | Local-main Gentle AI handoff + resolver contract + fallback | pi-source-checkouts, review-mode | §8 | ~400 | Revert; no gentle-pi/gentle-ai edits |
| WU-18 | RDD + secret-free web config + interactive auth | review-mode, web-search-config, pi-configuration | §10.1, §10.2 | ~350 | Revert |
| WU-19 | sdd-research override + Pi-specific retirement harness | sdd-research-override, security-boundaries | §10.3 | Historical estimate only | Revert; harness is isolated-HOME only |
| WU-20 | Acceptance harness, transition config, docs, cutover evidence | acceptance-verification, staged-migration | §13.2, §14, §19 | ~350 | Revert |

---

## WU-0 — Baseline safety evidence (read-only)

- [x] Capture the dirty-worktree baseline in the apply session log: `git status --porcelain`, `git stash list`, current branch and `git rev-parse HEAD` for the authorized workspace, plus read-only porcelain-status counts for the catalog-selected Gentle AI and Gentle Pi dependency checkouts. Every later unit re-checks against this baseline; dependency checkouts are observation-only and never edit surfaces. <!-- sdd-owner: implementation -->
- [x] Confirm the transition guard passes before any change: `bash -n apply bin/alex-cachyos-webapp-launch lib/*.sh modules/*.sh && python3 -c 'import json; from pathlib import Path; [json.load(p.open()) for p in Path("profiles").glob("*.json")]'` exits 0. <!-- sdd-owner: implementation -->
- [x] Confirm the documented start state: `test ! -f go.mod` and `test ! -d internal` succeed, so WU-1 begins from the design's stated baseline. <!-- sdd-owner: implementation -->

## WU-1 — Go scaffold, CLI surface, XDG state paths, runner config gate

Specs: convergence-commands (Preserved CLI surface), receipts (XDG state compliance), staged-migration (Transition testing configuration). Design §2.1, §6.1, §13.2.

- [x] RED `internal/cli/flags_test.go`: failing tests that `-h/--help` enumerates `--host --only --with --without --remove --dry-run --check --list`, unknown flags exit 2, and `--check` parses as a verify-only selection (parse level only). Evidence: `go test ./internal/cli/` fails because the package does not exist. <!-- sdd-owner: implementation -->
- [x] GREEN: create `go.mod` (module `alex-cachyos`), `cmd/alex-cachyos/main.go` (entrypoint + build metadata only), `internal/cli/command.go` with minimal flag parsing. Evidence: `go test ./internal/cli/` passes. <!-- sdd-owner: implementation -->
- [x] RED `internal/statepath/statepath_test.go`: XDG 0.8 defaults (`$HOME/.local/state`, `$HOME/.local/share`, `$HOME/.cache`) applied when variables are unset/empty; explicit `$XDG_STATE_HOME` override honored; runtime dir must be absolute, user-owned, mode-safe with `/run/user/<uid>` acceptance and **no `/tmp` fallback**; a table test asserts no returned path is under `~/.pi`. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/statepath/statepath.go` implementing the above. Evidence: `go test ./internal/statepath/` passes. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `internal/cli/command_test.go`: second-case coverage — `--list` prints the static module registry (`bootstrap, fingerprint, devtools, apps, vicinae, desktop, verify`), exit-code table (`75` reserved for lock contention, `2` usage) asserted. <!-- sdd-owner: implementation -->
- [x] REFACTOR: extract the exit-code table into `internal/cli/exit.go`; tests unchanged and green. <!-- sdd-owner: implementation -->
- [x] Update `openspec/config.yaml`: add `go test ./...` to `testing.runner`, `rules.verify.test_command`, and `testing.commands` **only after** `go test ./...` passes locally, retaining `bash -n …` and the profile JSON parse in the same command chain (staged-migration: both runners present once Go tests land). <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./...` exit 0; transition guard exits 0; `git status --porcelain` delta vs WU-0 baseline contains only unit-owned paths; record that exact path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, staging that exact path list explicitly and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-2 — sync-assets tool + embedded copies + drift check

Specs: staged-migration (Go CLI developed alongside with embedded copies), catalog (Embedded assets). Design §2.2.

- [x] RED `tools/sync-assets/main_test.go`: failing tests that `--check` exits non-zero when a declared copy under `internal/assets/data/` differs from its Bash source; the manifest `internal/assets/data/source-manifest.json` records path/size/SHA-256; undeclared source paths are never copied. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `tools/sync-assets/main.go` (copy declared paths byte-for-byte, normalize nothing, write manifest) and `internal/assets/assets.go` with `go:embed data`; seed the declared list with `catalog/` only (product copies are declared by later units as their behaviors land). Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `internal/assets/assets_test.go`: `//go:generate go run ../../tools/sync-assets` wiring; after `go generate ./internal/assets/...` a fresh `go run ./tools/sync-assets --check` exits 0; a manually perturbed copy makes it exit non-zero. <!-- sdd-owner: implementation -->
- [x] Update `openspec/config.yaml`: append `go run ./tools/sync-assets --check` to the verify/test chain (design §13.2 tier 2), retaining all previous entries. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; dirty-worktree baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-3 — Catalog schema, decode, pre-mutation validation

Specs: catalog (Versioned schema with pre-mutation validation; unknown module rejection). Design §3.1.

- [x] RED `internal/catalog/validate_test.go`: failing tests for rejection of unsupported `catalogVersion`, unknown top-level fields, unknown module names (registry = the seven Bash module names), references to missing overlay/template assets, and invalid checkout-pin shape; each error names the failure; a purity assertion shows validation performs no filesystem writes or OS port calls. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN: add `catalog/schema/catalog-v1.schema.json`, `internal/catalog/types.go` (presence-aware decode fields), `internal/catalog/decode.go` (unknown-field rejection, YAML → JSON-compatible data → schema → typed validation), `internal/catalog/validate.go`. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `testdata/catalog/galaxy-minimal.yaml`: a valid minimal catalog passes end-to-end; each invalid fixture under `testdata/catalog/invalid-*.yaml` produces a message naming the offending entry. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0 (schema dir now embedded via declared copy); transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-4 — Merge precedence, normalization, digest

Specs: catalog (Global-to-role-to-host merge precedence). Design §3.2.

- [x] RED `internal/catalog/merge_test.go`: failing tests for host > role > global on scalars/objects; empty role layer accepted; module-enable map merge by name; named records merge by `name`/`id` with same-layer duplicates failing; ordered lists replaced whole; `ListPatch` applied replace → remove → add with duplicate final identities failing; commands/steps replaced only as complete typed definitions by ID. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/catalog/merge.go` implementing presence-aware merge rules. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `internal/catalog/digest_test.go`: normalization sorts semantically unordered sets by stable identity while preserving explicit webapps/steps order; the normalized-JSON digest is identical across two runs and changes when content changes. <!-- sdd-owner: implementation -->
- [x] REFACTOR: consolidate merge rule table into one switch keyed by value kind; tests unchanged. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-5 — Source-specific pin grammar, host resolution, initial catalog authoring

Specs: catalog (Exact-version pinning only; Desired pins versus resolved versions; Host resolution without sensitive identifiers), pi-configuration (Exact-pinned Pi package set). Design §3.3, §3.4.

- [x] RED `internal/catalog/pins_test.go`: failing tests — npm ranges (`^`, `~`, `>=`, `latest`, non-exact tags) rejected; npm exact accepted; source checkout requires canonical remote + branch `main` + 40-hex commit; `pacmanArtifact` accepted only with approved cache/archive source + SHA-256 and never as the default for repository packages; AUR/local requires source commit + patch SHA-256; remote artifact requires SHA-256; relative checkout roots and unsafe/forbidden destinations rejected. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/catalog/pins.go` typed-by-source identity validation. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] RED `internal/app/host_test.go`: `--host` beats hostname; hostname resolution goes through the host port; unmatched hostname fails closed with an error listing known hosts before any mutation; the fake host port records that DMI, `/etc/machine-id`, and `/etc/machine-info` are never read. Evidence: failing test output; then GREEN in `internal/app/host.go`. <!-- sdd-owner: implementation -->
- [ ] Establish exact current versions by read-only observation (no invention): `pi list`, inspect `~/.pi/agent/npm`, query `origin/main` revisions through the read-only Git port for both catalog-selected dependency checkouts, run `pacman -Q` for the want-list, and inspect the fingerprint `_commit`/pkgrel/patch SHA-256 from `packaging/libfprint-egismoc-sdcp-git/`; record observations in the session log. Dependency checkouts remain outside the edit surface. <!-- sdd-owner: implementation -->
- [ ] Author the initial catalog from observations: `catalog/global.yaml`, `catalog/roles/workstation.yaml` (empty role, reserved), `catalog/hosts/galaxy.yaml` — each with `catalogVersion: 1` and `kind`, the approved exact npm inventory (`pi-commandcode-provider`, `pi-subagents-j0k3r`, `@juicesharp/rpiv-ask-user-question`, `pi-web-access`, `@juicesharp/rpiv-todo`, `pi-btw`, `pi-mcp-adapter`, `gentle-engram`, `pi-antigravity`, plus a local path package resolved from the catalog's named Gentle Pi checkout reference), source checkout pins, and module content mapping; validation must reject the build if any pin or resolved local-package path is incomplete. <!-- sdd-owner: implementation -->
- [ ] Repository-level validation test `internal/catalog/repo_test.go`: exactly one concrete host (`galaxy`); catalog declares its release name (`catalog-v1.0.0` reserved until the first real checkpoint) and digest; embedded copy of `catalog/` passes `sync-assets --check`. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; read-only statuses of both catalog-selected dependency checkouts are unchanged from baseline; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-6 — Planner, fake runner, galaxy fixture

Specs: planner (all requirements), convergence-commands (Dry-run is a plan, not an execution). Design §4.1, §13.1.

- [x] RED `internal/planner/plan_test.go`: failing tests for ordering invariants (`bootstrap` first, `verify` last, `apps` before `desktop`), cycle rejection at plan time, missing-dependency rejection, `--only` as exact selection that never silently adds modules, precedence `--only` > `--with/--without` > profile defaults, identical plans on repeated invocation, and plan digests excluding timestamps/temp paths/output/receipt IDs. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/runner/fake_test.go`: `FakeRunner` matches requests by operation + argv, records every invocation, mutates in-memory fixture state; a scanner test asserts no request uses a shell or contains `sudo`. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/planner/plan.go` (topological sort with module rank then lexical step-ID tie-break; immutable plan; typed inverse descriptors) and `internal/runner/{ports.go,fake.go}`. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `internal/planner/network_test.go`: `network: required` steps (pacman, AUR builds, icon fetches) are grouped and reported upfront by dry-run; file/template/config steps plan offline; network classification comes from the step type, never command text. <!-- sdd-owner: implementation -->
- [ ] Fixture `testdata/planner/galaxy-plan.json` + `internal/app/apply_test.go`: with fake runner, `apply --host galaxy --dry-run` produces the asserted plan (dependency order, `global → role → host` precedence, exact-pin resolution) with zero mutations; a second planned apply against the converged fixture yields no mutating steps (noChange). Evidence: `go test ./...` passes without any hardware requirement. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-7 — Receipt schema, atomic store, current index

Specs: receipts (all requirements). Design §6.2, §6.3.

- [x] RED `internal/receipt/schema_test.go`: golden `testdata/receipts/golden-v1.json` matching schema `alex-cachyos.receipt/v1` (steps, managedFiles, mutations, desiredPackages separate from `desiredExactPins`, distinct `desiredExactPins` vs `resolvedInstalledVersions`, checkouts, piRuntime, reviewMode, `credentials.referencedNames` only, `noChange`); rejection tests for credential values, biometric content, dirty-diff content, and secret-bearing argv. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/receipt/store_test.go`: receipt dir 0700, temp file 0600, fsync-file → no-replace rename → fsync-dir sequence; an existing receipt name is never opened for writing; `current.json` pointer updated by atomic rename (concurrent reader sees old or new, never partial); injected fsync/rename failures produce no partial receipt; a failed run still publishes its receipt; publication failure makes the command report failure without claiming an audit record. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/receipt/{schema.go,store.go}` using `internal/statepath` for the `$XDG_STATE_HOME/alex-cachyos/` layout. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: crash-injection between fsync and rename leaves no readable partial receipt; XDG-default path (`$HOME/.local/state/...`) asserted when `$XDG_STATE_HOME` is unset, override asserted when set, and no `~/.pi` path is touched in either case. <!-- sdd-owner: implementation -->
- [x] Docs fragment: `docs/receipts.md` — layout, one-immutable-receipt-per-run, atomic index, no-secrets/no-biometrics/no-diff contract (tests stay in this unit). <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-8 — Adoption backups, ownership manifests, inverse preconditions

Specs: convergence-commands (Detect-and-adopt, file part), security-boundaries (untouched runtime state, secret-file handling). Design §7.1, §6.4.

- [x] RED `internal/adopt/adopt_test.go`: failing tests — `<target>.bak.alex-cachyos` created exactly once via exclusive creation with preserved metadata, fsync, and SHA-256; adoption record binds target/backup/original hash+metadata/first receipt; a pre-existing backup without a matching valid record blocks adoption; unknown backups are never overwritten or guessed. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED inverse precondition tests (`internal/app/rollback_preconditions_test.go`): rollback refuses when live hash ≠ recorded after-hash; tool-created files removed only when the live hash still matches; adopted files restore the recorded one-time backup; package-owned files restore via package semantics (never claim the backup is package authority). Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED secret-file boundary test: an unmanaged `~/.pi/web-search.json` is handled metadata-only (existence check, never opened, never backed up, blocked with instructions to replace credentials manually); once configurator-created, SHA-256 comparison against embedded expected bytes without parsing content. Evidence: `go test ./internal/safefile ./internal/adopt ./internal/receipt -count=1` failed to compile on missing `ObserveWebSearch`, ownership metadata, trusted-reader, adoption `Lookup`, and receipt `Current` surfaces; GREEN/TRIANGULATE focused tests then passed with explicit unmanaged-read counters, owned-file digest-only comparison, unsafe-mode/symlink/size cases, and deterministic regular-file/symlink target-swap rejection. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/adopt/adopt.go` and inverse-descriptor support in `internal/planner`. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: second `adopt` on an already-adopted target is a no-op recorded in the ownership manifest; hostname-based profile suggestion returns `galaxy` for a matching hostname and a suggestion list otherwise. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Evidence: focused `go test ./internal/safefile ./internal/adopt ./internal/receipt -count=1 && go vet ./internal/safefile ./internal/adopt ./internal/receipt` passed; full Go, vet, Bash syntax, profile JSON, and embedded-asset checks passed. Unit-owned paths are `go.mod`, `go.sum`, `internal/safefile/**`, `internal/adopt/{adopt.go,security_test.go,web_search.go}`, `internal/receipt/{store.go,store_security_test.go}`, and these two cumulative OpenSpec ledgers. Rollback boundary: revert only those paths; tests use temporary directories and touch no host/Pi state. Staging/committing remains deferred and `git add -A` is forbidden. <!-- sdd-owner: implementation -->

## WU-9 — Non-destructive Git port: checkout algorithm, tags, isolated worktrees

Specs: worktree-safety (all requirements), pi-source-checkouts (Clone-if-missing adopt-if-present; Exact resolved commit recorded), catalog (Annotated catalog checkpoint tags). Design §7.2, §7.3.

- [x] RED `internal/gitx/checkout_test.go` (temporary repositories): clone-if-missing without force; adopt-if-present with canonical-remote verification (mismatch blocks); `git fetch` without pruning user refs or force-moving tags; `git status --porcelain=v1 -z` reduced to counts only; pin reachability from `origin/main` verified; changed-path intersection HEAD→pin vs local modifications aborts the step with bytes intact and `dirty:true`/`skipped:true` data; advancement uses plain `git checkout main` + `git merge --ff-only <pin>`; diverged local branches block. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/gitx/safety_test.go`: a scanner over every constructed Git command asserts absence of `--force`, `reset --hard`, `clean`, `stash`, and `commit`; prior-catalog reads use `git show`/`git cat-file` and never `git checkout <tag>` in the active tree; isolated worktrees are created detached under configurator-owned XDG state with an ownership marker; cleanup removes only marker-carrying, unmodified worktrees, otherwise leaves them. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/gitx/tags_test.go`: `checkpoint`-level tag creation as annotated (tagger + message), `git describe` visibility, refusal to move an existing tag without explicit force, and no configurator path emitting force. Evidence: failing test output; then GREEN `internal/gitx/{checkout.go,safety.go,tags.go,worktree.go}`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE `internal/gitx/update_test.go`: `apply --update` fetches `origin/main`, reports the exact newer candidate commit, and asserts the checkout ref, catalog bytes, and embedded manifest are unchanged. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; read-only statuses of both catalog-selected dependency checkouts are unchanged from baseline; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-10 — apply orchestration: lock, idempotency, elevation, dry-run

Specs: convergence-commands (Idempotent apply; Single-instance lock; Least-privilege elevation; Dry-run). Design §4.2, §4.3, §5.

- [x] RED `internal/app/lock_test.go`: exclusive `flock` at validated `$XDG_RUNTIME_DIR/alex-cachyos-<uid>.lock` for mutating commands; contention exits 75 before any mutation. Evidence: failing test output; then GREEN `internal/app/lock.go`. <!-- sdd-owner: implementation -->
- [x] RED `internal/executor/executor_test.go`: every mutating step runs Observe → Compare → Apply → Observe with an immediate pre-mutation re-observe; a satisfied step records `satisfied` and invokes no mutator; second apply on the converged fixture emits a receipt with `noChange: true` and executes no mutating step. Evidence: failing test output; then GREEN `internal/executor/executor.go`. <!-- sdd-owner: implementation -->
- [x] RED `internal/runner/exec_test.go` + elevation decorator test: system steps are wrapped exactly as `/usr/bin/pkexec <absolute-command> <argv...>`; user steps run unprivileged; the `sudo` scanner covers every real and fake `CommandRequest`; requests are argv-only with `shell=false`, env allowlist, timeout/output limits, and typed output policy. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN `internal/app/apply.go`, `internal/runner/exec.go`, elevation decorator, and the typed self-helper mode (normal process stages bytes + hashes, invokes the same binary via `pkexec` with a typed request; root side re-validates destination allowlist, source regular-file status, hash, mode, and operation type before atomic rename; accepts no general shell input) — proven against the fake elevation port. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: `apply --dry-run` performs local observations only, groups all `network: required` steps at the top, executes nothing, and leaves fixture state byte-identical; `check`/`--dry-run` never call the network port (assert). <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-11 — check / adopt / rollback / checkpoint / receipt command surfaces

Specs: convergence-commands (Non-mutating offline check; Rollback via inverse plan; Checkpoint command), catalog (tag recording in receipts), worktree-safety (non-mutating catalog reads). Design §5, §6.4, §7.3.

- [x] RED `internal/app/check_test.go`: non-mutating inventory — `niri validate`, `noctalia config validate`, byte-compare managed files to embedded templates, declared-package presence, greetd/boot/PAM compare, failed units, `pacman -Dk`/`-Qk`, `.pacnew` warnings; each drifted file reported with its `*.bak.alex-cachyos` backup path; fingerprint verification presence-only and warn-only; drift classes desired-state / post-apply / unmanaged conflict / unknown. Evidence: the pre-existing complete inventory suite plus RED `TestCheckCommandBuildsHostInventoryAndRunsOfflineChecker` (missing typed factory/handler), then focused GREEN `go test ./internal/app -run 'Test(Check|Command)' -v -count=1`. <!-- sdd-owner: implementation -->
- [x] RED `internal/app/commands_test.go`: `rollback --receipt <id>` replays inverses under preconditions and writes a new receipt (never edits the old one); `rollback --tag catalog-vX` reads prior catalog via object reads or an isolated worktree; system-level rollback reported as Snapper-delegated, not reimplemented; `checkpoint create --tag catalog-vX.Y.Z -m "..."` validates catalog/assets committed at HEAD and creates the annotated tag without committing unrelated dirty files; `receipt show` resolves `current.json`; `--remove` maps selected module steps to their inverse/removal plan. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] GREEN: wire subcommands `apply, check, adopt, rollback, checkpoint, receipt` in `internal/cli` + `internal/app`, with compatibility flags mapped as specified. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: fixture check with seeded drift reports file drift + backup path and exits non-zero; receipt records the applied catalog tag and release/digest; a concurrent second mutating command still exits 75. <!-- sdd-owner: implementation -->
- [x] Docs fragment: `docs/rollback.md` — inverse-plan rollback, preconditions, Snapper boundary, dirty-worktree guarantee; current fail-closed staged-migration limits are explicit. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-12 — Platform module factories: bootstrap + fingerprint

Specs: cachyos-platform (Module content mapped one-to-one; Explicit package ownership; Fingerprint package reproducible pinning). Design §11.

- [x] Read-only mapping of `modules/bootstrap.sh` and `modules/fingerprint.sh` into typed step inventories (want/remove lists, GRUB, Plymouth, ananicy-cpp, UFW, Chrome, zsh marker blocks, LTS deferral; PKGBUILD commit/pkgrel/patch SHA-256 from `packaging/libfprint-egismoc-sdcp-git/`); record the mapping in the session log. <!-- sdd-owner: implementation -->
- [x] RED `internal/platform/cachyos/bootstrap_test.go`: golden fake-runner plan — full-system update under the declared repository/transaction policy with recorded version changes, exact-name want/remove packages, explicit marking, atomic GRUB regeneration, Plymouth strip, ananicy-cpp, UFW, Chrome via paru, zsh marker-block rewrite, LTS deferral. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/platform/cachyos/fingerprint_test.go`: skip rebuild when pinned commit+pkgrel already installed (satisfied, no mutator); build/install via `pacman -U` only after source/patch checksum verification; PAM overlays with fprintd `sufficient`. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] GREEN `bootstrap.go`, `fingerprint.go`; extend `tools/sync-assets` declared copies with `packaging/`; wire both modules into the catalog with `dependsOn` ranks. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: idempotent re-plan reports satisfied dispositions; the plan records the transaction policy and version changes and performs no isolated implicit upgrade/downgrade outside it. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-13 — Platform devtools + apps + vicinae

Specs: cachyos-platform (Module content mapped one-to-one; Webapp install with icon fallback). Design §11.

- [x] RED `internal/platform/cachyos/devtools_test.go`: mise plus deterministic npm/pnpm configs with marker blocks, golden plan asserted. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/platform/cachyos/apps_test.go`: exact-name pacman inventory plus commit/checksum-pinned AUR/local sources; docker/tailscale/nordvpn services and groups; webapps from `name|url|icon_url` with Chrome `--app` launchers in `~/.local/bin`; remote icon failure yields the typed favicon-fallback outcome without failing the module. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [x] RED `internal/platform/cachyos/vicinae_test.go`: `vicinae-bin` installed from the catalog `sourceCommit`/`patchSHA256` through a verified local build, plus user service enable/start checks. Evidence: failing test output; then GREEN `devtools.go`, `apps.go`, `vicinae.go` with catalog wiring. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: idempotent observations for each module (satisfied on second plan); apps ordering dependency on desktop asserted via the planner invariants. <!-- sdd-owner: implementation -->
- [x] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-14 — Platform desktop + verify + explicit hardware gate

Specs: cachyos-platform (all remaining), acceptance-verification (Hardware verification gated on an explicit target). Design §11, §13.2 tier 4.

- [ ] RED `internal/platform/cachyos/desktop_test.go`: niri/Noctalia/greetd/AccountsService installed **before** Cosmic pruning; Cosmic prune carries explicit prerequisites (niri/Noctalia/greetd target checks satisfied) and typed externally-managed classification; PAM repair; sole Quickshell polkit agent (`qs -c polkit`); portals; Noctalia plugins; no X11 or alternative-DE entries anywhere in the plan. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED `internal/platform/cachyos/verify_test.go`: verify module always ordered last; biometric check is `fprintd-list` presence-only and warn-only; the inventory matches the check items already asserted in WU-11. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED hardware-gate test in `internal/app`: hardware-tier verification runs only when an explicit integration-target marker is declared; fixture success output is contractually distinguished from hardware convergence (asserted string/exit-code contract; a generic VM is never accepted as an integration target). Evidence: failing test output; then GREEN `desktop.go`, `verify.go`, gate flag. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: full seven-module galaxy golden plan snapshot regenerated and asserted; extend `tools/sync-assets` declared copies with `overlays/galaxy/` (bounded generated diff). <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

2026-09-01 bounded WU-14.1–14.3 continuation: the authoritative package list, role/polkit/session files, production-capable package/file ports, and exact-target desktop validators are implemented and tested. None of the broad WU-14 rows above is checked yet: Cosmic removal/PAM/plugins/portals, the complete WU-11 check inventory, and the full seven-module/overlay acceptance proof remain pending. <!-- sdd-owner: implementation -->

## WU-15 — Pi exact packages, user-owned renders, 23-route dual rendering

Specs: pi-configuration (Exact-pinned Pi package set; Desired pins versus resolved Pi versions; User-owned cataloged files), model-routes (all requirements). Design §9.1, §9.2.

- [ ] RED `internal/pi/packages_test.go`: catalog declares the full exact npm inventory plus the local package resolved from its named Gentle Pi checkout reference; the path package resolves relative to `~/.pi/agent/settings.json` and is verified to equal the catalog-selected read-only checkout without copying; installs are catalog-driven (`pi update --all` is never the convergence path); check probes both `~/.pi/agent/npm` and legacy `~/.pi/npm` and records which layout the installed Pi uses; desired-vs-resolved drift is reported without redefining pins. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED `internal/pi/render_test.go`: minimal `settings.json` shape (model/provider/thinking/theme/packages/changelog marker only — no `session_resources`, `default_mode`, `enable_continue`, `debug`, `model_profiles`); changelog marker is runtime-owned resolved metadata excluded from desired-state drift; canonical JSON (stable key order, one trailing newline); `@HOME@`/`@USER@` token validation before any write. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED `internal/pi/routes_test.go`: single typed source of 23 routes `{name, model, level}`; `subagents.json` renders `{model, effort}` plus `session_resources: lean`, `default_mode: task`, `enable_continue: false`, `debug: false`; `models.json` renders `{model, thinking}`; exactly the approved 23 names in both files; cross-file equality of name/model/level for all routes. Evidence: failing test output; then GREEN `internal/pi/{packages.go,render.go,routes.go}`. <!-- sdd-owner: implementation -->
- [ ] RED persona/background renders `internal/pi/persona_test.go`: `~/.pi/gentle-ai/persona.json` and `~/.pi/gentle-ai/background-subagents.json` byte-match catalog-rendered expectations. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: hand-edit one route level in a fixture `models.json` → `check` reports the drifted route with file and expected value; both files byte-compare to rendered expectations. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; `~/.pi` untouched in tests (fixture homes only); baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

Target-independent partial evidence (2026-09-01): `internal/pi` now has a typed
exact-pin package/install/probe boundary and canonical fixture-driven renderers
for minimal settings, the exact 23 dual routes, persona, and background state.
No WU-15 row is advanced: the repository still lacks authoritative production
versions for all nine npm packages, production `{model, level}` assignments,
and production persona/background values. The synthetic fixture proves the
generic contract only; it is not machine authority, live evidence, or WU-15.6
integration.

## WU-16 — Managed-asset refresh, AGENTS/APPEND ownership, forbidden-path centralization

Specs: pi-configuration (AGENTS.md user-owned, APPEND_SYSTEM.md package-managed; Package-managed assets verified, never vendored), security-boundaries (No secret values anywhere). Design §9.3, §7.1, §12.

- [ ] RED `internal/pi/assets_test.go` (installer behind a port; the real adapter is wired in WU-17): manifest schema parse rejects unsafe paths, duplicates, and non-SHA-256 values; entry-by-entry hash verification of regular non-symlink targets; force refresh replaces only content whose current hash proves package ownership; user-modified content is skipped and dropped from managed ownership. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED ownership tests: `~/.pi/agent/AGENTS.md` renders as user-owned from catalog guidance; a scan test proves the configurator exposes no write path to `APPEND_SYSTEM.md` (centralized forbidden-path list) and never copies the combined file; post-sync verification checks package markers and manifest ownership instead. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] GREEN `internal/pi/assets.go`; centralize forbidden paths (auth.json, mcp.json/mcp-cache.json/models-store.json, sessions, caches, `/var/lib/fprint`, `APPEND_SYSTEM.md`, `~/.pi/npm` node_modules, `.engram/chunks`, `.atl`, `.codegraph`) in one module exercised across filesystem, backup, hash, render, and command adapters; error messages identify the category, never content. Evidence: tests pass. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: vendoring rejection — a proposed plan that copies captured-machine managed assets fails catalog ownership validation with the install-output rule named. <!-- sdd-owner: implementation -->
- [ ] Docs fragment: `docs/pi-reproduction.md` — user-owned vs package-managed split, `AGENTS.md`/`APPEND_SYSTEM.md` boundary. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-17 — Local-main Gentle AI handoff: build, registration, resolver-selected execution, fallback

Specs: pi-source-checkouts (Integrity-preserving local-main runtime handoff; Post-install verification; Skip-install escape hatch fails closed), review-mode (resolver-selected execution contract). Design §8.

- [ ] RED `internal/pi/localmain_test.go` (fake build runner + local-source fixture): detached tool-owned worktree at the exact catalog commit (submodules rejected unless cataloged); build with `GOTOOLCHAIN=local`, `GOWORK=off`, `-mod=readonly`, `-trimpath`, `-buildvcs=true`, `main.version=main@<commit12>`; `go version -m` verification; staged-candidate attestation via its known absolute path — `review capabilities` show `vcs_revision` = full commit, `vcs.modified=false`, regular non-symlink executable, self-reported SHA-256 equals independently computed digest. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED publication + registration tests: `alex-cachyos.gentle-ai-main-build/v1` manifest carries canonical remote/branch/commit/tree/Go version/GOOS/GOARCH/CGO/flags/binary SHA-256; atomic publish to `$XDG_DATA_HOME/alex-cachyos/gentle-ai/main/<commit>/` (matching bundles reused, mismatching bundles block, never overwritten in place); strict `{"schema":"gentle-pi.dev-binary/v1","path":...}` written atomically at `~/.pi/gentle-ai/dev-binary.json` with the adoption contract for a prior configurator-owned registration and a block on unknown user registrations. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED resolver contract tests `internal/pi/resolver_test.go`: production resolver calls the bounded Node adapter (imports only the selected checkout's `resolveGentleAiBinary`/dev-registration and `installSddAssets` surfaces; fixed operation names; absolute paths; JSON out) immediately before **every** operational call — post-registration probes, `sync`, `review mode enable/status`, retirement probes; executor requires an absolute regular non-symlink path with the cataloged digest and an allowed resolver source; a scanner proves no `gentle-ai` logical name, `exec.LookPath`, shell, or PATH lookup anywhere; a conflicting ambient `GENTLE_PI_GENTLE_AI_DEV_BINARY` is allowed only when it resolves to the same path+digest, otherwise convergence/check blocks; disappearing adapter exports fail closed. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED fallback tests: `pnpm install --frozen-lockfile` runs with postinstall intact (never setting `GENTLE_PI_SKIP_GENTLE_AI_INSTALL=1`) so the signed v2.4.0 bundle stays installed and verified; rollback removes/restores only the configurator-owned registration after a hash precondition, then resolves the signed fallback through the same resolver; with the skip variable set, native-review semantics fail closed with `package-local-binary-missing` and the run is never reported converged; checkout alone is never reported as making the runtime consume `main`. Evidence: failing test output; then GREEN `internal/pi/{resolver.go,localmain.go,adapter/}`. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: post-install verification — package-local binary version and `managed-assets.json` integrity verified and recorded in the receipt (`piRuntime` fields); mismatch fails closed. <!-- sdd-owner: implementation -->
- [ ] Optional local-repo integration tier (isolated tool-owned worktrees only): build the exact cataloged commits and run resolver/manifest smokes; assert read-only statuses of both selected dependency checkouts are unchanged afterward. Record evidence in the session log; dependency checkouts are never edited. <!-- sdd-owner: implementation -->
- [ ] Docs fragment: `docs/pi-reproduction.md` local-main handoff section (supported hook, provenance chain, signed fallback). Verify and prepare the work-unit diff with the standard verification chain; staging/committing is deferred to a separate explicit user authorization (Commit/PR deferral). <!-- sdd-owner: implementation -->

## WU-18 — RDD enable/verify, secret-free web config, interactive-auth boundary

Specs: review-mode (all requirements), web-search-config (all requirements), pi-configuration (Interactive auth is never cataloged), security-boundaries. Design §10.1, §10.2.

- [ ] RED `internal/pi/rdd_test.go` (fake resolver): the flow explicitly invokes `review mode enable --scope global --cwd <declared-repo>` through the resolver-selected absolute path; `review mode status --cwd <repo>` must parse to `effective:on` + `deciding:global` after enable; every call records the same path/digest; prior state is read via the status API only (never its backing file); a disabled result is reported truthfully as `disabled/unmanaged` with no fabricated approval; `check` repeats status-only. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED `internal/pi/websearch_test.go`: credential references limited to `$NAME`, `${NAME}`, and `!command` with absolute executable; literal credential fields rejected at catalog validation and render time; rendered file mode 0600; receipts record referenced names only; template never overrides the documented env equivalents (`OPENAI_API_KEY`, `EXA_API_KEY`, `GEMINI_API_KEY`, `PERPLEXITY_API_KEY`, `CLOUDFLARE_API_KEY`) with literals; an explicit reference failing to resolve makes that provider fail locally with no stale fallback. Evidence: failing test output; then GREEN `internal/pi/{rdd.go,websearch.go,auth.go}`. <!-- sdd-owner: implementation -->
- [ ] RED auth boundary test: across apply/check fixtures the fs port records zero opens of `auth.json`, keyring, MCP credential files, caches, or sessions; authentication state is assessed only via documented status/capability probes returning `available|missing|unknown`, with non-allowlisted output discarded. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: receipts from RDD/web-search fixture runs contain credential names and no values (schema-level rejection already in WU-7 is exercised end-to-end here). <!-- sdd-owner: implementation -->
- [ ] Docs fragment: `docs/pi-reproduction.md` post-apply interactive steps — Codex `/login`, Kimi `/login kimi-coding`, browser-cookie opt-in, future OAuth. Verify and prepare the work-unit diff with the standard verification chain; staging/committing is deferred to a separate explicit user authorization (Commit/PR deferral). <!-- sdd-owner: implementation -->

## WU-19 — sdd-research override, Pi-specific retirement predicate, provisioned runtime harness

Specs: sdd-research-override (all requirements), security-boundaries (issue-status network evidence only, no secrets). Design §10.3.

- [ ] RED override render test `internal/pi/override_test.go`: `~/.pi/agent/subagents/sdd-research.md` rendered with exact front-matter (`name: sdd-research`, `model: commandcode/deepseek/deepseek-v4-flash`, `thinking: max`, allowlist `read, grep, find, edit, write, web_search, source_check, fetch_content, get_search_content, mem_search, mem_get_observation, mem_save`) and body carrying `gentle-ai.sdd-research/v1`, `gentle-ai.sdd-preproposal/v1`, and admission grants `documentation=[fetch_content]`, `open-web=[web_search, source_check, fetch_content, get_search_content]`; catalog encodes `retire_when: "pi-sdd-research-web-capable"`. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED predicate-core tests: all-of evaluation — GitHub status of `gentle-ai#3846` and `gentle-pi#471` closed/completed (recorded with retrieval time + URLs), the consumed exact `gentle-pi/main` commit contains the packaged agent, a strict parser finds the four web tools or cataloged one-to-one equivalents, non-empty `documentation` grants containing `fetch_content` and non-empty `open-web` grants containing all four, and the installed package agent hash matches `managed-assets.json`; truth-table test proves a generic Gentle AI v2.5 stable alone (and frontmatter-only allowlists, prose claims, incomplete tool lists) evaluates false. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED no-model registration/admission harness `internal/pi/harness/` + `testdata/pi/harness/`: isolated private `HOME`/XDG/Pi directories; minimal settings containing the catalog-exact `pi-web-access` plus the local package resolved from the selected Gentle Pi checkout reference; package/extension set installed or materialized; candidate packaged agents installed through the selected Gentle Pi asset installer; real Pi extension loader started with a bounded verifier extension that on `session_start` reads `pi.getAllTools()`, requires registered tool identities for the four web tools, queries the production admission loader for the effective `sdd-research` set, requires the admitted intersection, writes canonical JSON, and exits before any model request; missing extension install, loader failure, duplicate/renamed tools, or absent admission evaluates false. Evidence: failing test output. <!-- sdd-owner: implementation -->
- [ ] RED transactional retirement tests: harness must pass **before** removal; the override reference is backed up in the inverse plan; the live override is removed; resolver-selected `sync` runs; assets refresh; manifest verifies; the same registration/admission assertions repeat against the real agent home; any failure restores the override atomically and records a failed retirement; offline `check` uses the last receipt's issue observation, reports `unknown` when freshness is unavailable, and reports override staleness vs the predicate. Evidence: failing test output; then GREEN `internal/pi/{override.go,harness/...}`. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: predicate false on each individual missing condition (issues open, tools absent, grants empty, manifest mismatch, harness failure) with the override still installed in every case. <!-- sdd-owner: implementation -->
- [ ] Verify and prepare the work-unit diff: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0 (harness assets embedded via declared copies); transition guard exits 0; real `~/.pi` untouched in tests; baseline unchanged; record the unit-owned path list and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

## WU-20 — Acceptance harness, transition config finalization, docs, cutover evidence

Specs: acceptance-verification (all requirements), staged-migration (Cutover gated on validation evidence; Bash rollback fallback; Transition testing configuration). Design §13.2, §14, §19.

- [ ] Tier-1 anchor test `internal/acceptance/acceptance_test.go`: one `go test ./...` entry point asserting the galaxy dry-run fixture, merge order, exact npm/source/artifact pins, pacman name-plus-transaction policy, plan order, no-op second apply, receipt behavior, and Pi rendering — with a traceability index mapping each of the 15 specs' scenarios to its covering test name. Evidence: `go test ./internal/acceptance/ -v` lists the full mapping. <!-- sdd-owner: implementation -->
- [ ] Finalize `openspec/config.yaml` transition state: `bash -n …` + profile JSON parse + `go test ./...` + `go run ./tools/sync-assets --check` all retained and passing (staged-migration: both runners continuously validated). <!-- sdd-owner: implementation -->
- [ ] Docs finalized in English with quick-path + checklist structure: `docs/go-migration.md`, `docs/catalog.md`, `docs/receipts.md`, `docs/pi-reproduction.md`, `docs/rollback.md`. <!-- sdd-owner: implementation -->
- [ ] Cutover evidence report `docs/cutover-evidence.md`: fixture evidence vs hardware convergence kept distinct; hardware checks listed as gated on the declared integration target only; explicit statement that this change deletes no Bash files and cutover is a separate future proposal requiring fixture + check/dry-run + receipt/manifest evidence on the target. <!-- sdd-owner: implementation -->
- [ ] Walk all 15 spec scenario sets: mark each scenario covered-by-test or explicitly hardware-gated/pending-integration-target in the evidence report; any uncovered non-hardware scenario is a task-list defect to fix before apply completes. <!-- sdd-owner: implementation -->
- [ ] Final unit verification: `go test ./... && go vet ./... && go run ./tools/sync-assets --check` exit 0; transition guard exits 0; `git status --porcelain` matches the WU-0 baseline plus unit-owned paths only; record the full unit-owned path inventory and `git diff --stat` as the bounded diff handoff. Staging/committing is deferred: it happens only after a separate explicit user authorization, with explicit unit-owned paths and never `git add -A`. <!-- sdd-owner: implementation -->

---

## Bounded review and lifecycle gates (parent-owned)

- [ ] If and when the user separately authorizes branch/PR construction, review each behaviorally cohesive unit for unrelated scope and reviewer risk; no numeric line cap applies. Without that authorization, no branches or PRs are created. <!-- sdd-owner: parent -->
- [x] Ask-on-risk chain gate resolved: the user selected `stacked-to-main`. Apply may proceed; branch/PR construction remains blocked until the user separately requests it. <!-- sdd-owner: parent -->
- [ ] Review execution follows the user's enabled RDD switch and the harness's native authority at implementation time: check the effective review mode first; when disabled, run no reviews and report `disabled/unmanaged`; never fabricate review authority and never auto-review every unit merely because this plan exists. When enabled and the native flow directs it, start or reuse bounded review scoped to that unit's diff and its RED/GREEN/TRIANGULATE/REFACTOR evidence. <!-- sdd-owner: parent -->
- [ ] Confirm lifecycle gates before any cutover discussion: fixture suite green, transition validation green, evidence report present, receipts/tags verified — cutover itself remains a separate proposal and is out of scope for this change. <!-- sdd-owner: parent -->
