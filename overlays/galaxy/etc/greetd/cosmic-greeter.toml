[terminal]
vt = "1"

[general]
# Must stay "login". "cosmic-greeter" skips pam_systemd → the greeter
# (and COSMIC, if you pick it) panics with RuntimeDirNotSet.
service = "login"

[default_session]
command = "cosmic-greeter-start"
user = "cosmic-greeter"
