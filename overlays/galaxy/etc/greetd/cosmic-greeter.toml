[terminal]
vt = "1"

[general]
# Must stay "login". "cosmic-greeter" skips pam_systemd → cosmic-comp
# panics with RuntimeDirNotSet and the login screen never appears.
service = "login"

[default_session]
command = "cosmic-greeter-start"
user = "cosmic-greeter"
